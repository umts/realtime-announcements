# frozen_string_literal: true

source 'https://rubygems.org'

gem 'audio-playback', '~> 0.0'
gem 'whenever', '~> 0.9'

group :developement do
  gem 'rspec'
  gem 'rubocop', '~> 0.49'
  gem 'webmock'
end
